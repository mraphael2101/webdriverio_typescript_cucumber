namespace App2 {

    /* In this example, we have two files in the same namespace. This
    * is achieved by encapsulating both file contents inside a namespace
    * with the same name */

    export interface Draggable {}

    export interface DragTarget {}

}