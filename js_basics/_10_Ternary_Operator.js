
let runner = "diff"
let myExp = runner === "diff" ? "yes" : "no"
console.log(myExp)



// Nested ternary operator
runner = ""
let loglevel = runner === "diff" ? "yes" : runner === "" ? "a" : "b"
console.log(loglevel)


