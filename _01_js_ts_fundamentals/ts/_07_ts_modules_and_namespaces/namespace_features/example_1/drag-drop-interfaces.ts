namespace DDInterfaces {

    /* Adding the export keyword to the declaration makes the type accessible
    externally from the namespace which would otherwise not have been the case.

    It also makes the type accessible outside the namespace.*/

    export interface Draggable {}

    export interface DragTarget {}

}


